{
  "name": "@silyze/coordinator",
  "version": "1.0.0",
  "description": "A service state coordinator and log collector interface",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "keywords": [],
  "author": "Mihail Mojsoski",
  "license": "MIT",
  "type": "commonjs",
  "devDependencies": {
    "typescript": "^5.8.3"
  },
  "scripts": {
    "watch": "npx tsc --watch",
    "build": "npx tsc",
    "prepare": "npm run build",
    "version": "git add -A lib",
    "postversion": "git push && git push --tags"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@silyze/logger": "^1.0.1",
    "@silyze/typed-event-target": "^1.0.0"
  }
}
